import { SxStyles } from 'core'

export const styles: SxStyles<'form'> = {
  form: {
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
    height: '100%',
    justifyContent: 'space-between',
  },
}
