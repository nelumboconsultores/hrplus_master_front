import { SxStyles } from 'core'

export const styles: SxStyles<'container' | 'typography'> = {
  container: { display: 'flex', justifyContent: 'center' },
  typography: {
    maxWidth: '600px',
    textAlign: 'center',
    marginTop: '100px',
  },
}
